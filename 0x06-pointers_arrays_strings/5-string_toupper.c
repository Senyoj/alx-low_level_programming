#include "main.h"

/**
 * string_troupper -> converting any string to upper case
 * @X: string param
 * Return: string
 */

char *string_toupper(char *X);
{
	int i = 0;

	while (X[i])
	{
		if (X[i] >= 97 && x[i] <= 122)
			X[1] = x[i] - 32;
		i++
	}
	return (X);
}
