#include "main.h"
#include <unistd.h>
/**
 * _putchar - writes the character c to stdout 
 * Return: 1
*/
int _putchar(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
